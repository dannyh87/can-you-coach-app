DATABASE_URL="postgresql://postgres:postgres@localhost:5432/can_you_coach?schema=public"

# Required in production so generated invitation links use the canonical app domain.
# Optional locally; local development and tests fall back to http://localhost:3000.
APP_URL=""

# Optional locally. Required in production for controlled Clerk sign-in.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=""
CLERK_SECRET_KEY=""
NEXT_PUBLIC_CLERK_SIGN_IN_URL="/sign-in"
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL="/"
NEXT_PUBLIC_CLERK_SIGN_UP_URL="/sign-in"
